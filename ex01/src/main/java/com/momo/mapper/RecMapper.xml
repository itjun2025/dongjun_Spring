<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<!-- mapper의 namespace속성은 연결할 mapper의 interface 풀 경로로 작성  -->
<mapper namespace="com.momo.mapper.RecMapper">

<select id="getlist" resultType="com.momo.vo.RecBoardVO">

	select * from rec_board

</select>

<insert id="insert">

insert into rec_board (B_NO, title, nickName, regdate, updatedate
						, boomup, intro, cookTip, viewCnt, star, mno, C_NO)
        	   values (#{B_NO}, #{title}, #{nickName}, #{regdate}, #{updatedate}
        				, #{boomup}, #{intro}, #{cookTip}, #{viewCnt}, #{star}, #{mno}, #{C_NO})

</insert>

<delete id="delete">
		delete rec_board where com_bno= #{com_bno}
</delete>


<select id="getOne" resultType="com.momo.vo.ComBoardVO">
		select * from com_board where com_bno=#{com_bno}
	</select>

<update id="update">

update com_board
	set com_title = #{com_title}
		   		, title = #{title}
		   		, nickname = #{nickname}
		   		, regdate = #{regdate}
		   		, update_date  = sysdate
		   		, replycnt = #{replycnt}
		   		, mno = #{mno}
		 where com_bno = #{com_bno}  

</update>

<select id="getTotal" resultType="int">
	select count(*) from rec_board
</select>


</mapper>